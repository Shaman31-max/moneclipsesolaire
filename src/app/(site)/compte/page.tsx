import ComptePortal from "@/components/ComptePortal";

export const metadata = {
  title: "Mon Compte — MonEclipseSolaire.fr",
  robots: { index: false, follow: false },
};

export default function ComptePage() {
  return <ComptePortal />;
}
